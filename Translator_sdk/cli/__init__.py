"""Command-line interfaces for the Translator SDK.

The umbrella command is :data:`Translator_sdk.cli.main.cli`, exposed here as
``cli``. It is also reachable as the ``translator`` console script and via
``python -m Translator_sdk.cli``.
"""
from .main import cli as cli
